# Normalize line endings so Windows crawl runs don't rewrite whole data files
# (prevents huge, noisy diffs on data/*.jsonl and predictions/*.jsonl).
* text=auto eol=lf
*.jsonl text eol=lf
*.json  text eol=lf
*.py    text eol=lf
*.md    text eol=lf
*.bat   text eol=crlf
